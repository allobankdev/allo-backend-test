server:
  port: 0  # Random port for testing

spring:
  application:
    name: allo-backend-test

frankfurter:
  api:
    base-url: https://api.frankfurter.app
    timeout: 30000  # 30 seconds for tests (API might be slow)

github:
  username: RadityaDito

logging:
  level:
    com.allo.backend.test: INFO
    org.springframework.web: WARN
